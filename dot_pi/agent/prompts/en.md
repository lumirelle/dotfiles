---
description: Correct my English and answer
argument-hint: "<your message in English>"
---
The text below is from a native Chinese speaker learning English. Treat it two ways: as prose to correct toward what a native speaker would say, and as a request to respond to.

> $@

Correct only prose — grammar, spelling, punctuation, word choice, phrasing, naturalness. Leave code, commands, file paths, and identifiers untouched.

When the prose can be improved, open your response with a diff block in this exact shape, then a blank line, then your answer:

```diff
- <original line>
+ <native-speaker line>
```

- One `-`/`+` pair per changed line; omit unchanged lines.
- Fold every improvement into the single `+` line.
- Preserve the meaning; prefer the most natural phrasing over the literal grammar fix.
- No heading or explanation around the diff — the diff alone is the teaching.

When the prose is already natural and correct, skip the diff and answer directly.
